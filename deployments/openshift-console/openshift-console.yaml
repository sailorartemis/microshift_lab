"# This file was split into separate manifests:
# - namespace.yaml
# - serviceaccount.yaml
# - secret.yaml
# - clusterrolebinding.yaml
# - deployment.yaml
# - service.yaml
# - route.yaml
#
# Apply them together with:
#   kubectl apply -k deployments/openshift-console
#
"
