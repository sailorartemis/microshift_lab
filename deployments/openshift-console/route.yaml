apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: openshift-console
  namespace: openshift-console
spec:
  host: "openshift-console.apps.microshift.lab"
  to:
    kind: Service
    name: openshift-console-service
    weight: 100
  port:
    targetPort: 9000
  wildcardPolicy: None
